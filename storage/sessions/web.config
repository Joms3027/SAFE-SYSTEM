<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <system.webServer>
        <!-- Deny all access to session files -->
        <security>
            <requestFiltering>
                <hiddenSegments>
                    <add segment="sessions" />
                </hiddenSegments>
            </requestFiltering>
        </security>
        
        <!-- Disable all handlers -->
        <handlers>
            <clear />
        </handlers>
        
        <!-- Disable directory browsing -->
        <directoryBrowse enabled="false" />
    </system.webServer>
</configuration>
