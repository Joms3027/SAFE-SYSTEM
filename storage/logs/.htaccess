# Deny all web access to log files
Order deny,allow
Deny from all

# Prevent directory listing
Options -Indexes

# Block all file types
<FilesMatch ".*">
    Order Allow,Deny
    Deny from all
</FilesMatch>

