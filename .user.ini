; PDS form has many repeatable sections (children, education, experience,
; voluntary work, learning/development, references) that can easily exceed
; PHP's default max_input_vars=1000 when filled extensively.
max_input_vars = 5000
